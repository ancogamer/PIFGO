package funcs

// TODO
// HashPass hash the pass, so when in memory we dont have the original pass
func HashPass(pass string) (out string) {
	return
}

// Crypt encrypt file
func Crypt(data []byte, pass string) (out []byte) {
	out = data
	return
}

// Decrypt decrypt file
func Decrypt(data []byte, pass string) (out []byte) {
	out = data
	return
}
